作业1：
- 文本分类和NER属于NPL中的两类任务，当数据过完模型（bert），如果是分类任务，一般会将(B,L,H）通过取CLS或者POOLing变化为（B,H）然后通过分类层映射到num_lables;NER任务一般会将（B,L,H）进行展开，得到（BxL,H）对每个token进行分类；loss函数一般使用cross entropy
- 联合训练的方式有可能会导致一个loss下降很多，另一个完全没有下降，最后总loss下降，这样会导致一类任务的完成不好，解决方式就是通过添加权重，目的是使两个loss都下降才能使联合训练的loss下降来解决
