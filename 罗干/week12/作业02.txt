问题01:
前后端分离，就是将前端用户看到的界面，操作的动作与后端的业务逻辑就行独立分开，详细如下：前端它负责页面的布局、样式、动画效果以及用户操作（如点击、输入）的响应。后端: 专注于处理业务逻辑和数据。它负责与数据库打交道，进行数据的增删改查、权限验证、复杂的计算等，然后通过接口将处理好的数据(json或者xml)提供给前端。
问题02:
历史对话存在了关系型的数据库中；
在下一次用户提问时，系统会读取历史： 从存储系统中读取当前会话的历史对话列表，然后 将历史对话列表与用户最新的提问拼接在一起发送给模型。